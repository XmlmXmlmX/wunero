apiVersion: v1
kind: Service
metadata:
  name: {{ include "wunero.fullname" . }}
  labels:
    {{- include "wunero.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wunero.selectorLabels" . | nindent 4 }}
